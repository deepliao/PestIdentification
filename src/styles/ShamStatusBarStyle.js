import {StyleSheet,}from 'react-native'

// status状态栏
const ShamStatusBarStyle = StyleSheet.create({
    ShamStatusBar:{
        height:15,
        backgroundColor: '#63B8FF',
    },
    lookBigPictureShamStatusBar:{
        height:15,
        backgroundColor: 'black',
    }
});

export {ShamStatusBarStyle};